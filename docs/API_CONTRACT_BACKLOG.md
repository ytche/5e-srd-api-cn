# API 契约下一轮 Backlog（阶段G产出）

## B1. 集成层契约守卫

目标：让契约在集成层可感知，不仅是模型/控制器层。

任务：

1. 新增 `src/tests/integration/contract/` 用例
2. 覆盖：列表结构、详情字段、404 JSON、500 语义
3. 在 CI 中增加集成契约 job（可与 integration 合并或单独）

验收：

- PR 中可见集成契约检查结果

---

## B2. 非核心资源契约定义（图片接口）

目标：明确非 JSON 接口的错误约定，避免主契约冲突。

任务：

1. 在契约文档补充 `/api/images` 专章
2. 定义错误返回（文本/JSON）与状态码策略
3. 补 `imageController` 契约测试

验收：

- 文档与行为一致
- 前端/调用方可明确处理策略

---

## B3. 契约变更发布流程标准化

目标：让字段变更可追踪、可审计。

任务：

1. 在 release note 模板增加“API Contract Changes”段
2. 对破坏性变更要求单独条目
3. 关联 `docs/API_CONTRACT_GUARDRAILS.md` 的检查结果

验收：

- 每次接口变更 PR 都包含契约变更记录

---

## B4. 2024 资源契约化（暂缓）

状态：暂缓，不在当前迭代范围。

后续任务（恢复排期后执行）：

1. 盘点 `src/models/2024` 字段与 `API_CONTRACT` 差异
2. 补充 2024 必要双语字段（若数据库有）
3. 校验 2024 子资源 404/500 语义一致性
4. 为 2024 新增 `src/tests/contract/2024.*.test.ts`
